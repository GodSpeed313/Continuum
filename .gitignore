# Letta / Jetta agent session data
.letta/

# Claude local settings (skills are tracked; everything else local)
.claude/*
!.claude/skills/

# M7 GO-2 single-use gate: procedural attempt-record evidence (checklist §D.5) —
# runtime/session evidence, not governed code or a tracked governance artifact.
moltbook/.execution_state/

# Python
__pycache__/
*.pyc
*.pyo
*.pyd
.env
*.egg-info/
dist/
build/
.venv/
venv/
